<div class="m-4">
    <!-- <div class="row mb-3">
        <div class="col"></div>
        <div class="col">
            <a class="btn btn-outline-dark btn-block" href="#" role="button">Link</a>
        </div>
        <div class="col"></div>
    </div> -->

    <div class="d-flex justify-content-center  mb-3">
        <a class="btn btn-outline-dark btn-block" style="width: 20rem;" href="/updateRoom" role="button">Add</a>
    </div>
    
    <table class="table table-hover">
    <thead>
        <tr>
         <!--<th scope="col">ID</th>-->
        <th scope="col">Image</th>
        <th scope="col">Room Title</th>
        <th scope="col">Location</th>
        <th scope="col">Price</th>
        <th scope="col"></th>
        <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        {{#each rooms}}
        <tr>
        <!--<td>{{this._id}}</td>-->
        <td>{{#if this.image}} <img src= "/images/{{room.image}}" class="room-pic"> {{/if}}</td>
        <td>{{this.title}}</td>
        <td>{{this.location}}</td>
        <td>{{this.price}}</td>
        <td><a class="btn btn-outline-dark" href="/updateRoom/{{this._id}}"role="button">Edit</a></td>
        <td><form id="deleteButtonForm" method="POST" action="/admin/delete/{{this._id}}?_method=DELETE">
        <input type="submit" class="btn btn-outline-dark" 
        value="Delete" data-id="{{this.id}}" data-title="{{this.title}}">
        </form>
        </td> 
        </tr>
        {{/each}}
    </tbody>
    </table>
</div>
